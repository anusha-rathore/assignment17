#include<stdio.h>
int main(){
    char str[5] ={'a','n','u'};
    int i;
    for(i=0; str[i]!='\0';  i++)
    {
        // printf("%c",str[i]);
    }
    printf("the length of the string is : %d",i);
}